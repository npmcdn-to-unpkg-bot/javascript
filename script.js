 
 
// var eingabe = prompt("Bitte gib eine Zahl ein.");
 
 //alert("Du hast " + eingabe + " eingegeben.");

 /*
  * längerer Kommentar
  * 
  * */
 
 //d3.select("body").transition().style("color", "white");
 
 d3.select("body").style("background-color", "orange");
 
 var x=5, y;
 
 x *= 2; // x=2*x
 
 y = Math.sqrt(2);
 
 if (eingabe > 10) {
        x++;
     
 } else {
        x--;
        alert(x);
 }
